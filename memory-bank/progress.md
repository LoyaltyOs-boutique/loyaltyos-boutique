# Progress — LoyaltyOS

> Mirror of `.superpowers/sdd/progress.md` per permanent rule. Updated after every task.

## Timeline

- **2026-08-06:** Downloaded superpowers 607KB (codeload.github.com workaround for proxy 127.0.0.1:8892); audited `C:\Mould_AI_Agency` as **15% spec-driven**; `git init`; cloned to `C:\Mould_AI_Agency-SpecDriven\` (excluded `superpowers/` + `superpowers.zip`); converted to **100% spec-driven** with ledger, specs, plans, `.clinerules`, memory-bank (6 files), `tasks.md`, `.worklogs/`, `.gitignore`; stored permanent rule in 3 places.

## Task Status (from implementation plan)

| Task | Description | Status |
|---|---|---|
| 1 | Scaffold monorepo, DB schema, Docker Compose | ⏳ Pending (superseded by Convex amendment) |
| 2 | Points engine service (TDD) | ✅ Complete (`e2ff5b9`, 30/30 tests) |
| 3 | Auth middleware + merchant login | ✅ Superseded — done as Step 3 on Convex |
| 4 | Dashboard endpoints | ⏳ Pending (Step 4+) |
| 5 | Customers (CRM) CRUD + confidential records | ✅ Complete (Steps 4 + 4.5, commits 87d6c68 + a53958a) |
| 6 | Atomic order creation + points accrual/redemption | ⏳ Pending (Step 7, Module 7) |
| 7 | Settings endpoints | ✅ Complete (Steps 5 + 5.5) |
| 8 | Insights endpoints | ⏳ Pending |
| 9 | Campaigns + reach estimation + send | ⏳ Pending |
| 10 | Lookbooks + import stubs | ⏳ Pending (Step 6, Module 2) |
| 11 | Customer Portal API | ⏳ Pending |
| 12 | Frontend scaffold (Vite PWA, tokens, routing) | ✅ Complete (Phase 0, Ma'am's frontend intact) |
| 13 | Frontend: Dashboard, Settings, Portal home | ⏳ Pending |
| 14 | Frontend: CRM, POS, Campaigns | ⏳ Pending |
| 15 | Final whole-branch review + polish | ⏳ Pending (Step 10) |

## Log

- 2026-08-06: Spec-driven conversion complete. Permanent rule stored in `.clinerules`, `memory-bank/systemPatterns.md`, `memory-bank/activeContext.md`. Implementation awaits HARD-GATE approval to begin coding.
- 2026-08-06: Task 2 complete — points engine (`apps/api/src/services/points.js`) + TDD suite (`apps/api/test/points.test.js`, 30/30 pass) in commit `e2ff5b9`.
- 2026-08-07: **Convex amendment approved** (`docs/superpowers/specs/2026-08-07-convex-amendment-design.md`) — backend provider changed from Express+PG+Redis+Docker to Convex (Dec #2/#3/#8). This supersedes the original Mould Express/PG implementation plan for backend work.
- 2026-08-07: **Step 1 complete** — Convex project `loyaltyos-boutique` created in team `loyaltyos-boutique` (CLI slug `loyoltyos-boutique`; `wowcirclemould`/`perfect-skunk-360` was the OLD training account, not used), `convex/` scaffolded, env in `.env.local` (gitignored), branch `feat/step1-connect-convex` pushed. Tester agent: convex/ ✅, VITE_CONVEX_URL ✅, build CSS 27.74 kB ✅, `/login` HTTP 200 ✅ (commits `cf2cdd4`, `f90f9d1`). No `src/` edits.
- 2026-08-07: **Step 2 complete** — `convex/schema.ts` written from PRD §6 (users, lookbooks, catalogue_items, orders, campaigns — 5 tables, 5 indexes, paise integer money) and pushed to the correct deployment `pleasant-cobra-560` (team `loyaltyos-boutique`, dashboard https://dashboard.convex.dev/t/loyaltyos-boutique); verified via `npx convex data` (all 5 tables live) + `npx convex dev --once` (5 indexes added). Build stays **27.74 kB CSS**; no `src/` edits, no `.env.local` in git. Branch `feat/step2-convex-schema` pushed (commits `53e0d79`, `1255b70`). Preview: https://loyaltyos-boutique-rho14woih-loyalty-os1.vercel.app (state=success).
- 2026-08-07: **Step 3 complete** — secure auth per PRD §3.1/§3.2 in `convex/auth.ts`: `merchantLogin` (by_email + `bcrypt.compareSync` + 256-bit session token 7-day), `generateMagicToken` (by_mobile + 256-bit token + `magic_token_created_at`), `validateMagicToken` (by_magic_token + id match + 180-day expiry), `forgotPassword` (256-bit reset token 24h, mock email log, anti-enumeration). Schema: `users.by_email` index + `magic_token_created_at`/`reset_token`/`reset_expiry`/`session_token`/`session_expiry`. bcryptjs installed (Convex-side only; `src/` untouched). Deployed `pleasant-cobra-560`; tester verified live: merchant login owner@boutique.in/owner123 → 64-hex token + user (wrong pw/unknown → null); magic link `https://loyaltyos-boutique-three.vercel.app/lookbook?id=…&token=…` valid → user, wrong token → null, >180d → null; forgotPassword ok:true both known/unknown + RESET LINK logged. Build **27.74 kB CSS**. Branch `feat/step3-merchant-auth` (`687062b`, `0fab65d` fix: async bcrypt setTimeout → compareSync). No `src/` edits, `.env.local` untracked.
- 2026-08-07: **Step 3.5 complete** — frontend auth wired to Convex. `src/main.jsx`: `<ConvexProvider client={convex}>` wraps `<App/>`, client shared via `setConvexClient`. `src/lib/db.js`: same sync signatures (Login.jsx `const u = merchantLogin(...)`, guards `useState(getMerchantSession)`) but `merchantLogin` now fires `api.auth.merchantLogin` mutation in background → stores 256-bit Convex token (localStorage only token+id, never password); `generateMagicToken`/`validateMagicToken`/`forgotPassword` call `api.auth.*`; other db.js functions stay localStorage (Step 4.5 wires them). Live proof: `npx convex run auth:merchantLogin` → Convex users owner row gained `session_token=b245b872…` + `session_expiry=1786728430585` — data lands. Build **27.74 kB CSS**. Branch `feat/step3b-wire-auth` (`f87c2b1`), diff vs main ONLY `src/lib/db.js` + `src/main.jsx`, merged (`7557746..f87c2b1`); prod Vercel serves fresh bundle `index-45s7xkIU.js`, /login 200. No components/pages/data/index.css edits.
- 2026-08-08: **Step 3.6 complete** — Resend real email for forgot-password (PRD §3.1). `convex/auth.ts`: `sendResetEmail()` helper reads `process.env.RESEND_API_KEY`, sends from `LoyaltyOS Boutique <digital@mouldinnovation.com>` (subject "Reset your LoyaltyOS password - 85 Lansdowne", tokenized link `<base>/reset-password?id=…&token=…`, "Valid for 24 hours"); try/catch → falls back to mock console log so local dev never breaks; `{ ok: true }` anti-enumeration + 24h reset_expiry kept; generateMagicToken backup-channel comment (WhatsApp primary). Deps `resend ^6.18.1` + `@types/node ^26.2.0` (tsconfig unchanged). `npx convex dev --once` compile+push clean (3.38s). RESEND_API_KEY already set in Convex env (`re_CuZWh…`, validated HTTP 200; api.resend.com up). Live: known email → Convex-runtime egress to api.resend.com returns `application_error 'Unable to fetch data. The request could not be resolved.'` (3/3) → fallback fires, mock link logs fresh 256-bit token, `{ok:true}`, users row reset_token=908b36e4… rotated + reset_expiry now+24h; unknown email → bare `{ok:true}`. **Real delivery pending Convex egress recovery** (code+key+API verified). Build **27.74 kB CSS**. Branch `feat/step3c-resend-email` (`2b1d105`) — diff vs main ONLY `convex/auth.ts` + `package.json` + `package-lock.json`, zero `src/` edits; preview https://loyaltyos-boutique-iztz41xyh-loyalty-os1.vercel.app.
- 2026-08-08: **Step 3.6b complete** — ROOT CAUSE + fix: Convex mutations CANNOT fetch external APIs; only actions can. `forgotPassword` converted mutation → **action** (actions have NO ctx.db → flow: `internalQuery findMerchantByEmail` + `internalMutation saveResetToken` + `sendResetEmail` fetch — the sanctioned pattern). Compile+push clean (2.2s). **Live proof**: known email `owner@boutique.in` now returns Resend REAL response `validation_error: The mouldinnovation.com domain is not verified` (action+key+fetch work); fallback mock link logs fresh token + `{ok:true}`. Resend `/domains` → `data: []` (no verified domain = blocker); one-off sandbox email `cc54ad8a-413f-41b7-b1fb-2e1501d34733` (onboarding@resend.dev → digital@mouldinnovation.com) reached `last_event: "sent"` — pipeline delivers to brand Gmail. Unknown → bare `{ok:true}`. Build **27.74 kB CSS**. Branch `feat/step3c-resend-fix` (`7d747da` fix + `e23592c` ledgers) — diff vs main ONLY `convex/auth.ts` (59+/17-); Step 3.6 merged to main first (`cbb7678..dbac121`); preview https://loyaltyos-boutique-acbjout5l-loyalty-os1.vercel.app. **REMAINING (user action): verify `mouldinnovation.com` DNS at https://resend.com/domains → then real reset email sends from digital@mouldinnovation.com.**
- 2026-08-08: **Team handoff docs committed to main** — `STATUS_REPORT.md` + `TESTING_CHECKLIST.md` at repo root (outside `src/`), commit `249369d` on `main`. STATUS_REPORT: Done (Phase 0 5 commits + build sizes, Steps 1, 2, 3, 3.5, 3.6, 3.6b with branch names/commits/preview links/what was built + zero `src/` proof), Remaining (domain verification pending + Steps 4-10 per PRD Module 1 CRM Relationship Heat/Style/Type/Product Affinity/Delight Queue/birthdays, Module 2 Lookbook PDF/CSV/Instagram likes, Module 7 Billing Hybrid Checkout Razorpay mock/manual points/offline caching, Module 3 Reviews approval+point credit, Module 4 Support Tickets, Module 10 Final Integration GDPR/Token Validation/Security Audit/Deploy). TESTING_CHECKLIST: how far project done + 5 checks per step + all 3 main links (lookbook/join/login) + per-step previews with SSO caveat + GitHub commits + Convex deployment/dashboard + Resend dashboard + forgotPassword test procedure via Dashboard Functions Run. Zero `src/` edits.
- 2026-08-08: **Step 3.6 FINAL — Part A (merge + regression + anti-enumeration) complete** — merged `feat/step3c-resend-fix` into `main` (merge commit `7c09dd9`, pushed `ca3bc13..7c09dd9`; ledger conflict resolved). Vercel main auto-redeploys from `7c09dd9`. Regression: points TDD **30/30 PASS** (run from `C:\Mould_AI_Agency-SpecDriven\apps\api` — root repo has no `test` script); `npm run build` → CSS **27.74 kB** (gzip 5.61 kB), JS 345.17 kB, 2.91s; `git diff ca3bc13 HEAD -- src/ components/ pages/ lib/ data/` = **EMPTY**; `.env.local` untracked+gitignored; full-key grep `re_[A-Za-z0-9]{20,}` EMPTY in tracked files. Anti-enum: ghost@nowhere.in → `{ok:true}` + Resend log shows **NO ghost entry** (only earlier 905d51be owner@boutique.in bounced + cc54ad8a sandbox opened). **Part B (real-inbox delivery) PENDING one manual dashboard step:** insert test user with REAL Gmail (Convex Data → users → Insert, role merchant) → forgotPassword → confirm Resend delivered + Gmail inbox (check Spam).
- 2026-08-08: **Step 3.6 FINAL — Part B (Real-inbox delivery) NOT YET COMPLETE (blocked)** — 8/8 ~20:00 UTC+5.5 test: `forgotPassword {"email":"digital@mouldinnovation.com"}` → `{ok:true}` (correct) but Resend `/emails` shows **NO new send** and Convex `users` table has **ONLY 2 rows** (Priya Sharma customer + owner@boutique.in merchant) — the test-user row for digital@mouldinnovation.com was NOT inserted, so anti-enumeration correctly skipped the email (function only sends to existing merchant rows). NOT a code error. Pipeline-to-inbox proof already exists: sandbox `cc54ad8a` → digital@mouldinnovation.com has `last_event: "opened"`. **Unblock (1 click, 30s):** Convex dashboard → Data → users → Insert row {email: digital@mouldinnovation.com or any real Gmail, role: "merchant"} → save → rerun forgotPassword → expect Resend delivered/opened + Gmail inbox (check Spam). UI insert only per task rule — no code/CLI insert.
- 2026-08-08: **Step 3.6 FINAL — Part B COMPLETE** — 8/8 ~20:21 UTC+5.5 with real Gmail `mdsaidulmould@gmail.com` (merchant row `jd730h7z24n2ndhekdzs14mmpx8c2d9g`, inserted via dashboard): `forgotPassword {"email":"mdsaidulmould@gmail.com"}` → `{ok:true}` AND Resend `/emails` NEW entry `32e43c2d-c26a-4ca1-a688-d0c11da5b8d5`: to mdsaidulmould@gmail.com, from `LoyaltyOS Boutique <digital@mouldinnovation.com>`, subject "Reset your LoyaltyOS password - 85 Lansdowne", **`last_event: "delivered"`** — real reset email DELIVERED to Gmail from verified brand sender (mouldinnovation.com domain now verified; earlier validation_error gone). Token rotation: reset_token=7f4a6d60… fresh 256-bit + reset_expiry=1786287091431 (now+24h). Earlier digital@mouldinnovation.com no-send = missing-row anti-enum guard (not a code error). **Step 3.6 FINAL: ALL VERIFICATIONS PASS** — merge 7c09dd9, 30/30, 27.74 kB, src/ unedited, anti-enum, real delivery delivered.
- 2026-08-09: **Step 3.7 complete** — forgot-password UI wired to Convex action (client.action: Login.jsx mock link removed → `await forgotPassword(fpEmail, origin)`; db.js mutation→client.action), security fix `merchantLogin` now checks `u.password_hash` (wrong pw/unknown email → null, Login.jsx shows "Incorrect email or password."; commit `29f0dce`), real email delivered via Resend, build **27.74 kB CSS**, UI untouched. Branch `feat/step3d-wire-forgot-email`.
- 2026-08-10: **Step 4 complete** — Customer CRM backend (`convex/customers.ts`, 7 Convex functions, PRD Module 1): `getCustomers` + `getCustomerById` (merchant list/profile incl. confidential `measurements` + `staff_notes`, never auth secrets), `updateMeasurements` (partial-patch body-fit), `addStaffNote` ({text,date,author} append), `updateCustomTags` (replace tags — Experience-Based Segments), `getUpcomingBirthdays` + `getUpcomingAnniversaries` (Delight Queue: M-D/MM-DD parser, year-boundary aware, days_until sort). Contract parity with `src/lib/db.js` names for Step 4.5 UI wiring; zero `src/` edits; no schema change (fields already in users). Compile+push via `npx convex dev --once` clean; `convex/_generated/api.d.ts` regenerated (`customers` module). Build stays **27.74 kB CSS** (345.10 kB JS). Live-tested on `pleasant-cobra-560` via `npx convex run` — ALL 7: getCustomers returned Priya Sharma `jd7dkgsmw39fgpzqnhs2wys4v18c1077` (no secrets); getCustomerById profile clean; updateMeasurements persisted {bust 36, waist 28, hip 38, height 165cm, blouse_size M}; addStaffNote persisted (author Akanksha, date); updateCustomTags persisted [Hot Lead, Anniversary Gift, Kanjivaram] without clobbering measurements/notes; both queue queries ran clean (`[]` — no birthdays/anniversaries in seed data yet). Branch `feat/step4-customer-crm` (`87d6c68`) pushed; no `src/` edits.
- 2026-08-10: **Step 4.5 complete** — Wire CRM frontend to Convex, commit `a53958a` on feat/step4b-wire-crm, 7 functions bridged in db.js (`getCustomers`/`getCustomerById`/`updateMeasurements`/`addStaffNote`/`updateCustomTags`/`getUpcomingBirthdays`/`getUpcomingAnniversaries` -> `api.customers.*`), build 27.74 kB, components untouched. Step 4.5 verification: ① WIRE TESTS — `npx convex run customers:getCustomers` → Priya Sharma `jd7dkgsmw39fgpzqnhs2wys4v18c1077` (gold, 1250 pts, measurements/staff_notes/custom_tags, no auth secrets); `getUpcomingBirthdays '{"days":7}'` → `[]`; `getUpcomingAnniversaries '{"days":7}'` → `[]` (clean — no birthday/anniversary in seed, consistent with Step 4). ② MUTATION (staff note) — `npx convex run customers:addStaffNote '{"userId":"jd7dkgsmw39fgpzqnhs2wys4v18c1077","text":"Wire test Step 4.5","author":"Tester"}'` → returns Priya with `{author:"Tester", date:1786348466201, text:"Wire test Step 4.5"}` appended; `npx convex data users | findstr "Wire test Step 4.5"` → Priya's row `staff_notes` = [Akanksha note, Tester note] **persisted**. Branch pushed (`a53958a` already on origin/feat/step4b-wire-crm), working tree clean. Zero `src/` edits.
- 2026-08-11: **Improvement 4 complete** — WhatsApp number as UNIQUE key for customers (Ma'am's rule: ONE WhatsApp number = ONE customer profile, prevent duplicate accounts). convex/customers.ts gains 2 functions (existing 7 untouched): findCustomerByMobile (query: by_mobile index + role=customer filter → merchant view or null; trim+digits-only normalize) + createCustomer (mutation {mobile,name,birthday?,anniversary?,custom_tags?}: normalize → by_mobile uniqueness pre-check → existing → {ok:false, error:"This WhatsApp number already has a profile", existingId} NO insert → else insert {role:customer, points:0, tier:silver, custom_tags:[]} → {ok:true, id, customer}). npx convex dev --once push to pleasant-cobra-560 clean. Live tests: ① 9876543210 → Priya; ② unknown → null; ③ new 9876500001 "Test Unique" → {ok:true, id:jd786awf2qs05dhb334s52y21n8c9t8y}; ④ same mobile again → {ok:false} — DUPLICATE BLOCKED; ⑤ users table ONE row for 9876500001; ⑥ getCustomers OK (Priya + Test Unique). Build 27.74 kB CSS; git diff main -- src/ EMPTY. Branch feat/improve4-mobile-unique (9156ccf) pushed.
- 2026-08-11: **Step 5.5 complete** — Wire Settings frontend to Convex (owner settings now persist in Convex; Ma'am's Settings UI untouched). HARD-GATE design approval: main's db.js had ONLY synchronous `saveTierSettings(tierKey, patch)` (localStorage) — NO getSettings/updateTierConfig/updateTemplate/resetSettings; Settings.jsx reads `db.settings.tiers[global|silver|gold|platinum][purchasePercent|birthdayBonus|gmbPoints|productReviewPoints|on]` (a model Step-5's tier_rules validators rejected; no templates UI). User APPROVED Option 3 modified: align convex/settings.ts to the UI's REAL model, backend + db.js ONLY, ZERO Settings.jsx edits. `convex/settings.ts` rewritten clean+scalable: SETTINGS_KEYS singleton-per-key, TIER_KEYS global/silver/gold/platinum, `loyaltyRulesValidator` typed to the UI shape, DEFAULT_SETTINGS mirroring src/data/seed.js (purchasePercent/birthdayBonus/gmbPoints/productReviewPoints/on) + DEFAULT_TEMPLATES {name}, shared helpers getSettingsDoc/upsertSettings/readMergedSettings (no duplication). Public API: getSettings (query, defaults fallback), updateSettings (mutation — full UI-model payload → sanitized singleton loyalty_rules doc), updateTemplate (mutation), resetSettings (mutation). `src/lib/db.js` settings block + load-time call only: `saveTierSettings(tierKey, patch)` keeps sync local-first render → background `api.settings.updateSettings` write-through + refresh (mirrors merchantLogin/CRM bridge pattern); new `hydrateSettings()` (hydrates db.settings.tiers from api.settings.getSettings; seeds local values to Convex once when `updated_at.loyalty_rules === null`); async exports getSettings/updateTemplate/resetSettings → api.settings.*. Auth/CRM/catalogue/checkout/reviews/campaigns/onboarding untouched. `npx convex dev --once` push to pleasant-cobra-560 clean (2.38s); `npx convex codegen` confirmed types in sync. Build **27.74 kB CSS** (all 124 modules; api.settings.* resolves). Branch `feat/step5b-wire-settings`; diff vs main = convex/settings.ts + src/lib/db.js + regenerated convex/_generated/api.d.ts. **Tester VERIFIED (live, pleasant-cobra-560)** - updateSettings gold purchasePercent -> 7 persisted as loyalty_rules singleton; getSettings merges from Convex; updateTemplate welcome persisted; resetSettings restores defaults (table empty); CRM regression OK (Priya + Test Unique intact). diff vs main = convex/settings.ts + src/lib/db.js + ledgers; zero component/page/App.jsx/index.css/data edits.
- 2026-08-11: **Improvement 3b complete — TESTER VERIFIED** — Magic link / onboarding link opens ONBOARDING FORM (Join) instead of request-access page. AUDIT FIRST (HARD-GATE, approved): Lookbook auth useEffect set denied=true -> AccessDenied for ANY missing/invalid token; Join did NOT read query params; validateLookbook = local users id+magic_token match. IMPLEMENT: src/pages/Lookbook.jsx auth useEffect rewritten with useNavigate — id w/o token -> /join?id= (prefill); invalid/expired token -> /join?id=&token= (form); NO params -> invitation stays (public landing, code comment); valid token -> personalized lookbook (NO regression). src/pages/Join.jsx reads useSearchParams: fromLink context heading ("Complete your profile." vs "Join our lookbook.") + prefill name/WhatsApp from customerById(id). Build 27.74 kB CSS unchanged (JS 349.28 kB). E2E (puppeteer-core + Edge, preview :4173) ALL 4 PASS zero console errors: T1 invalid link -> /join form; T2 id-no-token -> /join?id=priya-b; T3 no-params -> invitation stays; T4 valid Priya -> "Namaste, Priya" + membership (regression safe). Diff vs main ONLY Lookbook.jsx + Join.jsx (49+/8-); index.css/data/convex/components/App.jsx untouched. Branch feat/improve3b-magiclink-to-form commit dfa656e pushed; Vercel preview auto-deploy.
- 2026-08-11: **Fix: Client Onboarding magic link opens client module directly — TESTER VERIFIED** — Convex validation for merchant-created clients. ROOT CAUSE (audit): Lookbook.jsx validated tokens ONLY against the local localStorage `users` table, so a client created on Convex via merchant Onboarding could never pass local `validateLookbook` — landing on the join form instead of their personal module. IMPLEMENT (design spec `docs/superpowers/specs/2026-08-11-fix-onboarding-magiclink-design.md` approved): `src/lib/db.js` refactor — core `createLocalCustomer` extracted (sync `onboardCustomer` contract unchanged) + new `onboardCustomerRemote` (Convex `customers:createCustomer` unique-per-WhatsApp + `auth:generateMagicToken` 256-bit backend token; falls back to local link when Convex unreachable) + `syncMagicLinkCustomer` (upsert local view keyed by Convex id, stamps validated token); `src/pages/merchant/Onboarding.jsx` async submit → `onboardCustomerRemote` + disabled "Minting secure link…" button; `src/pages/Lookbook.jsx` Convex fallback — when local validateLookbook fails, `auth:validateMagicToken` (180-day) → `syncMagicLinkCustomer` into local state → opens module DIRECTLY (NO /join redirect); truly invalid → `/join` form. Build **27.74 kB CSS** exact. E2E (puppeteer-core + Edge, preview :4173) ALL 5 PASS zero console errors: T1 Convex-created client → personal module direct; T2 invalid → form; T3 /join works; T4 invitation; T5 Priya lookbook (regression safe). Temp scripts + preview.log cleaned. Diff vs main ONLY `src/lib/db.js` + `src/pages/Lookbook.jsx` + `src/pages/merchant/Onboarding.jsx` (121+/8-); index.css/data/convex/components/App.jsx untouched. Branch `feat/fix-onboarding-magiclink`.
- 2026-08-11: Improvement 1 complete - eye button opens client lookbook in new tab (2b1fbd1)
- 2026-08-11: Improvement 2 complete - WhatsApp logo replaced with luxury share arrow icon (bdebff3)
- 2026-08-11: Improvement 3 complete - self-onboarding link share via WhatsApp in Onboarding (ef97ffc)
- 2026-08-12: Governance - memory-bank synced to Convex reality (58fab8a)
- 2026-08-12: Governance - Convex amendment completed (tier names, 5-table atomicity, Redis drop) (b7a1d2c)
- 2026-08-12: Governance - LEDGER RULES added to .clinerules (5431d99)
- 2026-08-12: Governance - baseline tag v1-baseline + current-state.md (edc5f63)
- 2026-08-12: Governance - docs hygiene fix (current-state HEAD, CRM task complete, tier/auth wording, gitignore) (f5041bb)
- 2026-08-12: Task <N>: complete (commits 2055831, review clean) — .clinerules updated with 5 new hard rules (ASK-BEFORE-EDIT, LEDGER-COMPLETE, NO-REDO, NO-UNREQUESTED-WORK, DUPLICATE-CHECK).
- 2026-08-13: **Step 6.2 complete** — Wire Merchant Catalogue frontend to Convex. 8 Convex lookbooks functions bridged in `src/lib/db.js` (`getLookbooks`/`getLookbookById`/`createLookbook`/`updateLookbook`/`deleteLookbook`/`addCatalogueItem`/`updateCatalogueItem`/`deleteCatalogueItem` -> `api.lookbooks.*`), local-first hydration bridge `hydrateCatalogue()` mirrors Step 3.5/4.5/5.5 pattern, build **27.74 kB CSS** exact. Zero component edits — Catalogue.jsx unchanged. Branch `feat/step6-lookbook` continues from Step 6.1 (`f100aa6`). Commits: `ad07b0a`.
- 2026-08-13: Step 6.3 complete (commits 3baa5bc..5b6e461, review clean) — Wire Join + customer lookbook to Convex; customer lookbook real products hydration; Join form Convex createCustomer + mobile-unique check. Build 27.74 kB CSS; diff vs main ONLY src/lib/db.js.
- 2026-08-13: Step 7.1 complete (commit fe1ae9b, review clean) — Orders backend (convex/orders.ts) with atomic order+points engine; verified via npx convex run; build 27.74 kB CSS; diff vs main ONLY convex/orders.ts.
- 2026-08-14: Task 8.1: complete (commits 3c8450b..HEAD, review clean) — Reviews backend (convex/reviews.ts + schema.ts) implemented with atomic create/approve/decline/point-credit; tested via npx convex run; build 27.74 kB CSS; diff vs main only convex/reviews.ts + schema.ts.
- 2026-08-14: Task 8.2: complete (commits 829c9fb, review clean) — Wire reviews frontend to Convex (db.js reviews functions -> api.reviews.*); hydration pattern; build 27.74 kB CSS; diff vs main only src/lib/db.js.
- 2026-08-17: Task 9: complete (commit f033b30, review clean) — Fix: magic link 180-day session - preserve magic_token_created_at through hydration + expiry check in validateLookbook.
- 2026-08-17: Task 2: complete (commits 53abcc6..8588abf, review clean) — Fix 2: Mobile number validation + India country code +91 normalization; new convex/mobile.ts helper; createCustomer/findCustomerByMobile/generateMagicToken normalized; Onboarding UI error display (invalid/duplicate mobile); build 27.74 kB CSS; diff vs main convex/mobile.ts + convex/customers.ts + convex/auth.ts + src/pages/merchant/Onboarding.jsx.
- 2026-08-17: Task 3: complete (commits 6d55c40, review clean) — Fix 3: Remove +91 prefix from stored mobile (plain 10 digits); backward-compat lookup matches both +91XXXXXXXXXX and 9876500001; createCustomer blocks duplicates, returns existingId; existing numbers get magic link (no duplicate)
- 2026-08-17: Task 4: complete (commits 758356e, review clean) — Improvement 2: Minimal 10-digit mobile validation (block garbage inputs); frontend alert on invalid; build 27.74 kB CSS.
- 2026-08-17: Task 10: complete (commit a235f3d, review clean) — Improvement 2 (fresh minimal): minimal 10-digit mobile validation in createCustomer (digits.length !== 10 → {ok:false, error}), stores mobile exactly as typed (no +91, no prefix); existing numbers keep magic link behavior; Onboarding/Join show inline red error for invalid numbers, generate magic link for existing; frontend validation mirrors backend; build 27.95 kB CSS exact. Diff: convex/customers.ts + src/pages/Join.jsx + src/pages/merchant/Onboarding.jsx.
- 2026-08-17: Task 11: complete (commit 421b7a8, review clean) — Fix: existing number returns ok:true isExisting - magic link flow works, no dup row. createCustomer now returns {ok:true, isExisting:true, existingId, customer} for existing numbers instead of {ok:false, error:"This WhatsApp number already has a profile", existingId}. Frontend db.js onboardCustomerRemote simplified (removes special error-string check) so both Onboarding.jsx and Join.jsx follow success path → generateMagicToken creates link for existing customer. Invalid numbers still {ok:false, error:"Please enter a valid 10-digit mobile number"} (red line). New numbers {ok:true, id, customer} unchanged. npx convex dev --once pushed to pleasant-cobra-560; build 362.61 kB / 107.60 kB gzip; branch feat/fix2-invalid-number pushed. Diff: convex/customers.ts + src/lib/db.js + design spec.
- 2026-08-17: Task 12: complete (commit 71099c3, review clean) — Fix: Join.jsx onboarding reference - self-onboarding magic link works. Import changed from `onboardCustomer` (local-only sync) to `onboardCustomerRemote` (Convex async with magic token generation) so Join self-onboarding matches Onboarding.jsx behavior. Invalid numbers → red line inline; valid new numbers → Convex createCustomer + generateMagicToken → personal module link; existing numbers → generateMagicToken for that customer (no dup) → personal module link. Build 27.95 kB CSS exact. Branch feat/fix-join-onboard pushed. Diff: src/pages/Join.jsx (1 line change).
- 2026-08-18: **Improvement 3 complete** — Customer CRM edit + save in Basic info tab (Ma'am luxury UI). `convex/customers.ts`: new `updateCustomerProfile` mutation (name, birthday, anniversary, tier — partial patch; mobile NOT editable, unique key). `src/lib/db.js`: `updateCustomerProfile(userId, patch)` bridge (background Convex write-through + local state hydration). `src/pages/merchant/Customers.jsx`: `isEditing` state + Edit button (btn-ghost, gold accent) in Basic info tab → inline inputs/selects (text-sm, border-line, focus:border-gold, bg-paper) prefilled with current values; mobile read-only with "unique - not editable" note; Save (btn-gold) calls bridge → view mode refreshes with updated data; Cancel discards changes. Build **27.95 kB CSS** (exact). Branch `feat/improve3-customer-edit` (commit 71f2721) pushed; PR https://github.com/LoyaltyOs-boutique/loyaltyos-boutique/pull/new/feat/improve3-customer-edit. Tester verified: open detail → Edit → fields editable → change name/birthday/tier → Save → view shows updated + Convex dashboard reflects; mobile NOT editable; Cancel reverts; other tabs (Profiling/Notes/Ledger/Points) unchanged; row list/search/eye/copy/link intact; zero console errors; design luxury intact (gold #C5A880, ink #111111, Playfair/Montserrat, tracking-luxe). Regression: merchant onboarding, join, magic link, checkout all unchanged (smoke).
- 2026-08-18: **Improvement 3 complete (Profiling tab + custom_tags fix)** — Fix Save button schema bug + add Boutique Profiling edit. `convex/customers.ts`: `updateCustomerProfile` args add `custom_tags: v.optional(v.array(v.string()))` — was missing, causing FunctionValidation error when Save clicked (custom_tags sent from UI but rejected by validator). `src/pages/merchant/Customers.jsx`: Profiling tab Edit/Save mirroring Basic info pattern — `isEditingProfiling` state + `editMeasurements` form; Edit button (btn-ghost) in Profiling header; Edit mode: number inputs for Bust/Waist/Hips (step 0.1), text for Height/Blouse size; Colours/Fabrics stay read-only (not in updateMeasurements args); Save calls `updateMeasurements` bridge → await → view refreshes; Cancel discards. Build **27.95 kB CSS** (exact). Branch `feat/improve3-customer-edit` (commit aacf0e1) pushed. Tester verified: Basic info Save now works (custom_tags persists to Convex); Profiling Edit → change Bust/Waist → Save → Convex measurements updated + view refreshed; Cancel both tabs discards; Mobile read-only; other tabs untouched; zero console errors; luxury design intact.
- 2026-08-18: **Improvement 4 complete** — Public lookbook view + copy/WhatsApp share (Ma'am's requirement: share lookbook to ANY person WITHOUT creating customer/magic link/self-onboarding; person sees products directly + WhatsApp inquiry). NEW: `src/pages/PublicLookbook.jsx` (read-only public view: lookbook title/designer + product grid with image/title/price + "Inquire via WhatsApp" per product; NO auth/customer session/magic link/bag/points/tier/reviews/checkout/personalization; luxury design identical to customer lookbook). `src/App.jsx`: new route `/lookbook/public/:lookbookId` -> PublicLookbook (public, NO guard); existing `/lookbook` route untouched (invitation + magic-link flow 100% unchanged). `src/pages/merchant/Catalogue.jsx`: per-lookbook Copy Link button (🔗 Copy Link → copies `${window.location.origin}/lookbook/public/${lookbookId}` + toast "Lookbook link copied") + WhatsApp Share button (reuses Customers.jsx gold share-arrow SVG exactly: `wa.me/?text=<elegant encoded message with the public URL>`). Build **27.95 kB CSS** (exact). Branch `feat/improve4-public-lookbook` (commit 70d67ca) pushed. T1: /lookbook/public/<existing lookbook id> opens WITHOUT login/token → shows products + WhatsApp inquiry ✅; T2: /lookbook (no params) → invitation page (ORIGINAL UNCHANGED) ✅; T3: valid magic link → personal module (ORIGINAL UNCHANGED) ✅; T4: invalid magic link → /join form (ORIGINAL UNCHANGED) ✅; T5: Catalogue → Copy Link copies public URL → incognito → public view shows products ✅; T6: Add new product to that lookbook → SAME public URL shows it (auto-updated, no link change) ✅; T7: WhatsApp Share opens wa.me with message + URL ✅; T8: Public view has NO points/tier/checkout/customer data (privacy — only products + inquiry) ✅; Console clean; luxury design; no regression anywhere.
- 2026-08-18: **Improvement 5 complete** — WhatsApp inquiry message shows customer NAME instead of ID (Ma'am's requirement: "Hi, I'm [Name]! I'm interested in the [Product] from [Lookbook]."). `src/pages/Lookbook.jsx`: `waLink` function refactored — removed hardcoded "My Customer ID is ${customer.id.toUpperCase()}" and now uses `customer.name` if available (`Hi${namePart}! I'm interested in the ${item.title} from 85 Lansdowne.`). Build **27.95 kB CSS** (exact). Branch `feat/improve5-inquiry-message` (commit dc28bc4) pushed. T1: Customer lookbook inquiry → message includes name if logged in, NO customer ID ✅; T2: Public lookbook unchanged (no customer session) ✅; T3: Build passes, no regression ✅; Console clean; luxury design intact.
- 2026-08-20: Fix: reviews schema missing catalogue_item_id: complete (commit 501a870, tester verified) — added `catalogue_item_id: v.optional(v.id("catalogue_items"))` to reviews table in convex/schema.ts, fixing dev server schema validation error on 1 of 15 review documents (real product-review data, not test junk — kept the field rather than deleting the data); npx convex dev --once deployed clean; npx convex data reviews --limit 15 confirms all 15 docs load, affected doc's value intact; direct to main per user approval (1-line schema-only change); git branch -vv confirms main [origin/main] 501a870.
- 2026-08-22: **Gate 1+2 branch complete (NOT YET MERGED to main — pending user testing/approval)** — commit `29ad025` on branch `feat/gate1-gate2-schema-foundation` (previous commit on branch: `b6ddbb2`). Bundles: (1) Schema foundation — `whatsapp_consent`/`is_deleted` on `users`, `size`/`colour` on `catalogue_items`, `kind` on `lookbooks` (all `v.optional`, existing rows unaffected). (2) Gate 1 CSV bulk customer import — new `convex/customers.ts` `bulkCreateCustomers` mutation (reuses `createCustomer`'s 10-digit + by_mobile duplicate-check logic, also skips in-file duplicate mobiles) + new "Import clients from CSV" card in `src/pages/merchant/Onboarding.jsx` (drag/drop, Name/WhatsApp/Birthday/Anniversary/City/Country columns, preview + confirm-before-commit). (3) Gate 2 Step A — lookbook-sharing rework per approved spec `docs/superpowers/specs/2026-08-22-lookbook-share-and-selector-design.md`: new `convex/lookbooks.ts` `getLookbooksForSelector` query (`{_id, name, kind}`, title relabelled to name); per-piece Copy Link/WhatsApp-share bug fix in `Catalogue.jsx` (was routing to the whole-lookbook public URL via `i.lookbook_id` instead of the piece's own id — confirmed via investigation before this fix); new `src/pages/PublicPiece.jsx` page at new route `/lookbook/piece/:pieceId` (registered in `src/App.jsx`, 2-line addition — user explicitly confirmed inclusion of this normally do-not-edit file before commit); lookbook/PDF selector dropdown; dummy "Buy Now" button (Coming soon alert) + real "Inquire" button (reuses Improvement 5 WhatsApp pattern). Verification: `npx convex dev --once` clean (backend half tested separately by office-backend-agent — see prior session); `npm run build` succeeds, CSS **28.48 kB** (up from branch baseline 28.10 kB, +0.38 kB from this branch's frontend additions — flagged, not hidden, exceeds documented ~27.74-28.10 kB range). Bulk-import backend tested via `npx convex run customers:bulkCreateCustomers` (2 created, 3 skipped — existing dup/in-file dup/invalid mobile — verified against `npx convex data users`). Pushed: `git push -u origin feat/gate1-gate2-schema-foundation`; `git branch -vv` confirms `feat/gate1-gate2-schema-foundation 29ad025 [origin/feat/gate1-gate2-schema-foundation]`. Vercel preview: https://loyaltyos-boutique-193ktnkt2-loyalty-os1.vercel.app (deployment succeeded). **NOT merged to main — stays on branch until user tests themselves and confirms.**
- 2026-08-21: **Gate 2 Step C pushed (NOT YET MERGED to main — pending user testing/approval)** — commit `b02cc2f` on branch `feat/gate1-gate2-schema-foundation` (previous: `11f4388`). Adds Manual Entry assign-to-designer-lookbook: `createLookbook` gains a `kind` param, `Catalogue.jsx` Manual Entry form gets an "Add to" selector (existing designer-lookbook or create-new). Also carries forward previously-uncommitted Gate 2 Step B work in this same push: PDF-lookbook upload backend (`convex/lookbooks.ts` `generatePdfUploadUrl` action + `createPdfLookbook` internalMutation via Vercel Blob, `convex/schema.ts` `lookbooks.pdf_url` field) and Step B frontend (`Catalogue.jsx` CSV/PDF file-type router, `db.js` `uploadPdfLookbook` bridge). **KNOWN OPEN ISSUE — PDF-upload bug, NOT fixed in this push, still pending diagnosis** (a real test file `MIRARI LOOKBOOK1.pdf`, 10MB, is sitting untracked at repo root as the reproduction artifact for that diagnosis — deliberately left untracked/uncommitted, not part of this commit). Files: `convex/lookbooks.ts`, `convex/schema.ts`, `docs/superpowers/specs/2026-08-22-lookbook-share-and-selector-design.md`, `package.json`/`package-lock.json` (`@vercel/blob`), `src/lib/db.js`, `src/pages/merchant/Catalogue.jsx` (7 files, +622/-11). Pushed: `git push origin feat/gate1-gate2-schema-foundation`; `git branch -vv` confirms `feat/gate1-gate2-schema-foundation b02cc2f [origin/feat/gate1-gate2-schema-foundation]`. Vercel preview: https://loyaltyos-boutique-na598mesm-loyalty-os1.vercel.app (deployment succeeded). **NOT merged to main — stays on branch until user tests and confirms, AND until the PDF-upload bug is diagnosed/fixed.**
